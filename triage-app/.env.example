# Backend (set in Render dashboard -> Environment)
ANTHROPIC_API_KEY=your_api_key_here   # optional: enables LLM plain-language guidance. Falls back to rule-based text if unset.

# Frontend (set in Vercel dashboard -> Environment Variables)
VITE_API_URL=https://your-backend-service.onrender.com
